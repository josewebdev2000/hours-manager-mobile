/* Async Storage keys to be used throughout the app */

// Create object for async storage keys
const asyncStorageKeys = {
    jwtToken: "JWT_TOKEN",
    jwtExpiryDate: "JWT_EXPIRATION_DATE",
    passwordResetTokenRequested: "PASSWORD_RESET_TOKEN_REQUESTED",
    passwordResetTokenValid: "PASSWORD_RESET_TOKEN_VALID",
    passwordResetToken: "PASSWORD_RESET_TOKEN"
};

export default asyncStorageKeys;