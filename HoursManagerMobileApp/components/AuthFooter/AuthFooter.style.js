/**
 * Styles for Footer where links will inhabit in Auth Pages
 */

import { StyleSheet } from "react-native";

const authFooterStyles = StyleSheet.create({
    footer: {
        marginTop: 20,
        alignItems: "center"
    }
});

export default authFooterStyles;